<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:resizeableActivity="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mining"
        tools:targetApi="31">

        <!--启动页-->
        <activity
            android:name=".activity.LaunchActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--主页-->
        <activity android:name=".activity.MainActivity" />
        <!--登录-->
        <activity android:name=".activity.login.LoginActivity" />
        <!--注册-->
        <activity android:name=".activity.login.RegisterActivity" />
        <!--usdt账单-->
        <activity android:name=".activity.user.UsdtBillActivity" />
        <!--修改资料-->
        <activity android:name=".activity.user.SetUserActivity" />
        <!--修改昵称-->
        <activity android:name=".activity.user.ModifyNameActivity" />
        <!--c2c交易市场-->
        <activity android:name=".activity.c2s.C2CActivity" />
        <!--c2c交易订单-->
        <activity android:name=".activity.TransactionOrderActivity" />
        <!--买入界面-->
        <activity android:name=".activity.c2s.BuyActivity" />
        <!--卖出界面-->
        <activity android:name=".activity.c2s.SellActivity" />
    </application>
</manifest>